name: Live Fight Stats

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run script
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_THREAD_ID: ${{ secrets.TELEGRAM_THREAD_ID }}
          EVENT_URL: ${{ secrets.EVENT_URL }}
          EVENT_ID: ${{ secrets.EVENT_ID }}
        run: python main.py

      - name: Commit and push state
        if: success()
        run: |
          echo "Текущая ветка: $(git branch --show-current)"
          echo "Статус до коммита:"
          git status
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add current_index.txt fight_ids.json live_message_id.txt 2>/dev/null || true
          git diff --staged --quiet && echo "Нет изменений для коммита" || (
            git commit -m "Update state" &&
            git push origin HEAD:main
          )
